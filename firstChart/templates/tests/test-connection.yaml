apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "firstChart.fullname" . }}-test-connection"
  labels:
    {{- include "firstChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "firstChart.fullname" . }}:{{ .Values.rstudio.service.rstudioPort }}']
  restartPolicy: Never
